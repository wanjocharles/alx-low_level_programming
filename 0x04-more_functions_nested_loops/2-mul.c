#include "main.h"
/**
 * mul - mutiplies two integers
 * @x: The first number to mutiply
 * @y: the seciond number to mutiply with first number
 * Return: The result of multiplication between x param and y param
 */
int mul(int x, int y)
{
return (x * y);
}
